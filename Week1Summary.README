There are a b two algorithms to implementation

For a, we have finished 1 and 2, had familar with the frame, programming in Python, and requirements

Both Naive Bayes and Logistic Regression has not yet talked in classes, which is good becasue if we prepare for it even for a little bit on assignment,
it would be much better to understand in class.

Two Week is still very tight for P2.
